echo "node_modules/" >> .gitignore

# untrack the folder (keeps it locally)
git rm -r --cached node_modules

# -------------------------------
# Node & Dependencies
# -------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
pnpm-lock.yaml
yarn.lock

# -------------------------------
# Build / Dist files
# -------------------------------
dist/
build/
.cache/
.eslintcache
vite.config.timestamp-*.mjs

# -------------------------------
# Environment & Secrets
# -------------------------------
.env
.env.local
.env.*.local
*.env
*.env.*


